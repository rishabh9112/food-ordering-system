services:
  # 1. Backend Web Service (Docker container)
  - type: web
    name: food-ordering-system-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    region: oregon
    plan: free
    envVars:
      - key: DB_URL
        sync: false # User will input their PostgreSQL connection URL (e.g. Neon)
      - key: DB_USERNAME
        sync: false
      - key: DB_PASSWORD
        sync: false
      - key: JWT_SECRET
        generateValue: true # Generates a random secure token key automatically
      - key: RAZORPAY_KEY_ID
        sync: false # User's Razorpay Key ID
      - key: RAZORPAY_KEY_SECRET
        sync: false # User's Razorpay Key Secret
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
      - key: PORT
        value: 8080

  # 2. Frontend Static Site (React Single Page Application)
  - type: web
    name: food-ordering-system-frontend
    runtime: static
    buildCommand: npm run build
    staticPublishPath: build
    rootDir: frontend
    envVars:
      - key: REACT_APP_API_URL
        value: ${FOOD_ORDERING_SYSTEM_BACKEND_URL}/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
